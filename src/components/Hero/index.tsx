import { Container } from '../../global'
import { ContainerDiv, Formulario, HeroTitle } from './styles'

const Hero = () => (
  <Formulario>
    <Container>
      <ContainerDiv>
        <HeroTitle>
          As melhores vagas para tecnologia, design e artes visuais.
        </HeroTitle>
      </ContainerDiv>
    </Container>
  </Formulario>
)

export default Hero
